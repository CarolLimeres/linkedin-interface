{
  "extends": "./tsconfig.json", // Extende a configuração base
  "compilerOptions": {
    // Outras opções específicas do aplicativo
  },
  "include": ["src/**/*"], // Inclui todos os arquivos dentro da pasta src
  "exclude": ["node_modules"]
}
